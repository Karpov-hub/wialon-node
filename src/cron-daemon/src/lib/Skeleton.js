import db from "@lib/db";
import Queue from "@lib/queue";

const time = "* * * * *";
const description = "Monthly comissions";

async function run() {
  
}

export default {
  time,
  description,
  run
};
